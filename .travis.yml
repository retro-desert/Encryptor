language: python
python:
  - 3.8
install:
  - pip install pycryptodomex
  - pip install pycryptodome
  - pip install colorama
  - pip install requests
script:
  - mkdir ~/testing
  - python encryptor_CMD.py --test -P ~/testing
